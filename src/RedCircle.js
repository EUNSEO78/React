const RedCircle = () => {
  const style = {
    width: "100px",
    height: "100px",
    borderRadius: "9999px",
    backgroundColor: "red",
  };

  return <div style={style}></div>;
};

export default RedCircle;
